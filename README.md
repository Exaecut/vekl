# Exaecut - Effects

## Subtrees

This repository uses git subtree (subtree) for dependencies (see `subtrees.json`).

Pull updates:
python scripts/subtree.py pull

Push changes:
python scripts/subtree.py push
